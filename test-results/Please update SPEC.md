Please update SPEC.md about making plan, use specialzed agents and make in autopilot mode, make log
1. Each todoitem can have item DeadLine, that could not be fill
2. Todoitem has to priority. User can add own priority.
3. Priority can use set, default is from low to high (nice to have, good be, must be     asap).  If it is not fill Deadline, the default is good be, otherwise must be)
4. API for data in this project (items, categories, label etc, can be accessed via API)
5. MCP server for this data, AI can manage 
6. Create Prompt row that via ChatGPT and MCP server will can make operation
7. Make tests 
8. Make test API, how many api access can be done during 

